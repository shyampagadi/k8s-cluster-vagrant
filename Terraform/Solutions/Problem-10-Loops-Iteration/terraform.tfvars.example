# Loops and Iteration - Count and For Each - Example Variable Values

# String Variables
project_name = "loops-demo"
environment = "development"
app_name = "web-app"
aws_region = "us-west-2"
vpc_cidr = "10.0.0.0/16"
ami_id = ""  # Leave empty to use data source

# Number Variables
development_instance_count = 1
production_instance_count = 3
database_port = 3306
log_retention_days = 30

# Boolean Variables
create_database = false
enable_monitoring = true
enable_encryption = true
enable_dns_hostnames = true
enable_dns_support = true
enable_versioning = false
enable_multi_az = false
block_public_access = true

# List Variables
availability_zones = ["us-west-2a", "us-west-2b", "us-west-2c"]
security_group_names = ["web", "app", "database"]
bucket_names = ["logs", "data", "backups"]
log_group_names = ["web", "app", "database"]

# Instance Type Variables
development_instance_type = "t3.micro"
production_instance_type = "t3.small"

# Map Variables for For Each
server_configs = {
  web1 = {
    instance_type = "t3.micro"
    disk_size     = 20
    monitoring    = true
    environment   = "production"
    tags          = { Role = "Web Server", Tier = "Frontend" }
  }
  web2 = {
    instance_type = "t3.micro"
    disk_size     = 20
    monitoring    = true
    environment   = "production"
    tags          = { Role = "Web Server", Tier = "Frontend" }
  }
  app1 = {
    instance_type = "t3.small"
    disk_size     = 50
    monitoring    = true
    environment   = "production"
    tags          = { Role = "App Server", Tier = "Backend" }
  }
  app2 = {
    instance_type = "t3.small"
    disk_size     = 50
    monitoring    = true
    environment   = "production"
    tags          = { Role = "App Server", Tier = "Backend" }
  }
}

# Complex List Variables for Dynamic Blocks
ingress_rules = [
  {
    from_port   = 80
    to_port     = 80
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  },
  {
    from_port   = 443
    to_port     = 443
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  },
  {
    from_port   = 22
    to_port     = 22
    protocol    = "tcp"
    cidr_blocks = ["10.0.0.0/8"]
  }
]

egress_rules = [
  {
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }
]

# Database Configuration Variables
database_engine = "mysql"
database_version = "8.0"
database_instance_class = "db.t3.micro"
database_name = "webapp"
database_username = "admin"
database_password = "changeme123"
database_allocated_storage = 20

# Backup Configuration Variables
backup_retention_period = 7
backup_window = "03:00-04:00"
maintenance_window = "sun:04:00-sun:05:00"

# Additional Configuration
additional_tags = {
  Owner       = "DevOps Team"
  CostCenter  = "Engineering"
  Backup      = "Daily"
  Compliance  = "Required"
  Project     = "Loops Demo"
}
