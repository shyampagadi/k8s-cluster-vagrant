[application]
name = "${project_name}"
environment = "${environment}"
vpc_id = "${vpc_id}"

[storage]
logs_bucket = "${storage_buckets.logs}"
data_bucket = "${storage_buckets.data}"
backup_bucket = "${storage_buckets.backup}"

[compute]
enabled = "${compute_enabled}"

[monitoring]
enabled = "${monitoring_enabled}"
