[application]
name = "${project_name}"
environment = "${environment}"
vpc_id = "${vpc_id}"

[storage]
bucket_name = "${bucket_name}"

[state_management]
backend_type = "s3"
state_locking = "enabled"
state_encryption = "enabled"
