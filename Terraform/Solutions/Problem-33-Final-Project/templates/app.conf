[application]
name = "${project_name}"
environment = "${environment}"

[final_project]
comprehensive_solution = "enabled"
enterprise_grade = "enabled"
production_ready = "enabled"
course_completion = "enabled"
